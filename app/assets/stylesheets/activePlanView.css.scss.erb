// Place all the styles related to the Activements controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/
/*
*= require deployment_clock
/*
* Same height as small map.
* It also makes the back link
*/
.activePlanView {
}

.activePlanRoutesView {
  height : 256px;
}
/* Display of a Route Code */
.item.route-selected {
  background: pink;
}
.item.route-highlighted {
  background: lightgray;
}
.item.route-invisible {
  display: none;
}
.item.rtype-journey .route-icon {
  background : url(<%= asset_path 'bus_icon_active.png' %>) no-repeat;
}
.item.rtype-route .route-icon {
  background : url(<%= asset_path 'route_icon.png' %>) no-repeat;
}
.item.route-hasactivejourneys .route-icon {
  background : url(<%= asset_path 'route_icon_active.png' %>) no-repeat;
}
.route-icon {
    height : 20px;
    width : 20px;
    float: left;
}

.route-code {
    text-align: right;
    width: 4em;
}

.route-name {
    text-align: left;
    padding-left: 1em;
    padding-right: 1em;
}
.route-times {
    text-align: right;
    margin: 2px;
    font-size: 6pt;
}

.route-times .route-time {
}


#deploy-ui-buttons {
  padding-top : 10px;
}

/**
 * From H2 of Twitter Bootstrap
 */
#deployment-header {
  float: left;
  font-weight: bold;
  font-size:24px;
  line-height:36px;
}
