<h1>Busme <%= @master.name %> <%= t('.title') %></h1>

<%= comfy_form_for @layout, :as => :layout, :url => cms_admin_site_layouts_path(:site_id => @site.id) do |form| %>
  <%= render :partial => 'form', :object => form %>
<% end %>
