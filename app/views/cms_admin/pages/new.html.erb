<h1><%= t('.title') %></h1>

<%= comfy_form_for @page, :as => :page, :url => cms_admin_site_pages_path(@site), :html => {:multipart => true} do |form| %>
  <%= render :partial => 'form', :object => form %>
<% end %>
