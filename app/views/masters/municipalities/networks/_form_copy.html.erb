
<%=  form_for( @network, :url => copyto_master_municipality_network_path(@master, @municipality, @network)) do |f| %>

    Copy <%= link_to(@network.name, master_municipality_network_path(@master, @municipality, @network)) %> to
    <%= f.collection_select(:municipality, @municipalities, :id, :name, {:disabled => @disabled_options}, {:class => "btn"}) %>
    <%= f.submit "Copy", :class => "btn", :disabled => @disabled %>
<% end %>