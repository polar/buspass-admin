<% content_for :head do %>

    <script type="text/javascript">
        $(function() {
            // Initialise the map
            init(<%= "[#{@center[0]},#{@center[1]}]" %>,
                    <%= "[#{@from[0]},#{@from[1]}]" %>,
                    <%= "[#{@to[0]},#{@to[1]}]" %>,
                 "<%= @kml.html_safe %>");
        });
    </script>

<% end %>