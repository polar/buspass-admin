
<%= link_to "Map", map_master_municipality_network_path(@master, @municipality, @network), :class => "btn" if muni_admin_can?(:read, @network) %>
<%= link_to "Plan Network", master_municipality_network_plan_path(@master, @municipality, @network), :class => "btn" if muni_admin_can?(:edit, @network) %>
<%= link_to "Copy to Deployment", copy_master_municipality_network_path(@master, @municipality, @network), :class => "btn" if muni_admin_can?(:read, @network) %>
