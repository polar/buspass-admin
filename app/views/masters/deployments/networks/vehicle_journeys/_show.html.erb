<h2><%= link_to @network.name, master_deployment_network_path(@master, @deployment, @network) %></h2>
<h3><%= link_to "Route #{@vehicle_journey.route.name}",
                master_deployment_network_route_path(@master, @deployment, @network, @vehicle_journey.route) %></h3>

<h3><%= link_to "Service #{@service.name}", master_deployment_network_service_path(@master, @deployment, @network, @service) %></h3>
<%= render :partial => "table_service" %>
<%= render :partial => "table_vehicle_journey" %>

<h4>Timing Links on this Vehicle Journey</h4>
<%= render :partial => "table_timing_links" %>