
<%= link_to "Map", map_master_deployment_network_path(@master, @deployment, @network), :class => "btn" if muni_admin_can?(:read, @network) %>
<%= link_to "Plan Network", master_deployment_network_plan_path(@master, @deployment, @network), :class => "btn" if muni_admin_can?(:edit, @network) %>
<%= link_to "Copy to Deployment", copy_master_deployment_network_path(@master, @deployment, @network), :class => "btn" if muni_admin_can?(:read, @network) %>
